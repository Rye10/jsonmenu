<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jsonmenu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuList"></activity>
        <activity android:name=".Menu1"></activity>
        <activity android:name=".Menu2"></activity>
        <activity android:name=".Menu3"></activity>
        <activity android:name=".Menu4"></activity>
        <activity android:name=".Menu5"></activity>
        <activity android:name=".Menu6"></activity>
        <activity android:name=".Menu7"></activity>
        <activity android:name=".Menu8"></activity>
        <activity android:name=".Menu9"></activity>
    </application>

</manifest>